// The item action interceptor, for a test that loads the event bridge without
// exercising flipeffects. Setting it does nothing, so nothing intercepts.

#include <trx/game/items/actions.h>

void ItemAction_SetInterceptor(const ITEM_ACTION_INTERCEPTOR interceptor)
{
}
