#pragma once

// How many objects a fake stands up. It reaches past every identity the
// executable names, so a test can use any of them, and it is the fakes' own
// number rather than the catalog's.
#define FAKE_OBJ_COUNT 1024
