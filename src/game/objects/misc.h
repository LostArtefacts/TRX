#ifndef T1M_GAME_OBJECTS_MISC_H
#define T1M_GAME_OBJECTS_MISC_H

#include "global/types.h"

void SetupCameraTarget(OBJECT_INFO *obj);
void SetupMovingBar(OBJECT_INFO *obj);
void SetupLeadBar(OBJECT_INFO *obj);

#endif
